# Déploiement de l’infrastructure

Le déploiement est réalisé via une seule commande :

docker compose up -d

Cette approche garantit une reproductibilité totale de l’environnement
sur n’importe quelle machine disposant de Docker.